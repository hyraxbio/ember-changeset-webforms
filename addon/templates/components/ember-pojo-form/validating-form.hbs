<form {{action "submit" on="submit"}} data-test-id="validating-form" class={{formValidationClass}} novalidate={{emberPojoForms.settings.novalidate}}>
  <div class="form-section form-fields">
    {{#each formFields as |formField|}}
      {{ember-pojo-form/validating-form-field
        data-test-validating-field
        formField=formField
        setFormFieldValue=(action "setFormFieldValue")
        setFormFieldError=(action "setFormFieldError")
        setFormFieldProperty=(action "setFormFieldProperty")
        customValidations=(action "customValidations")
        customTransforms=(action "customTransforms")
        afterKeyUpAction=afterKeyUpAction
        afterValidationAction=afterValidationAction
        data-test-id=(concat "validating-field-" formField.fieldId)
        changeset=changeset
      }}
    {{/each}}
  </div>
  {{yield}}
  <div class="form-section form-submit">
    {{ember-pojo-form/form-submit-button click=(action "submit" changeset) value=submitButtonText requestInFlight=requestInFlight data-test-id="evf-submit-form-button" class=formMetaData.submitButtonClasses disabled=formMetaData.submitDisabled}}
    {{#if formMetaData.showResetButton}}
      <button class={{formMetaData.resetButtonClasses}} type="button" data-test-id="evf-reset-form-button" onclick={{action "rollback" changeset}}>{{resetButtonText}}</button>
    {{/if}}
  </div>
</form>
