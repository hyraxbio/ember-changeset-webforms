{{#unless formField.hideLabel}}
  {{#if formField.legendComponent}}
    <legend>{{component formField.legendComponent}}</legend>
  {{else if formField.fieldLegend}}
    <legend>{{formField.fieldLegend}}</legend>
  {{/if}}
{{/unless}}