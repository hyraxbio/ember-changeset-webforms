{{#unless formField.hidden}}
  {{#if (or (eq formField.fieldType "input") (eq formField.fieldType "textarea")) }}
    {{component formField.component
      formField=formField
      inputIcon=inputIcon
      onFocusOut=(action "onFocusOut")
      onFocusIn=(action "onFocusIn")
      onKeyUp=(action "onKeyUp")
      changeset=changeset
      displayValidation=displayValidation
      displayValue=displayValue
    }}
  {{else}}
    {{component formField.component
      formField=formField
      inputIcon=inputIcon
      onUserInteraction=(action "onUserInteraction")
      setFieldProperty=(action "setFieldProperty")
      validateField=(action "validateField")
      changeset=changeset
      displayValidation=displayValidation
      displayValue=displayValue
    }}
  {{/if}}
{{/unless}}
  