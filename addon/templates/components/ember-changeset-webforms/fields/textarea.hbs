<textarea
  class="validation-area
    {{ember-changeset-webforms/dynamic-class-names
      'textareaElement'
      @changesetWebform
      @formField
      @formField.validationStatus
    }}"
  value={{@displayValue}}
  onkeyup={{fn (action "onUserInteractionAction") @formField "keyUp"}}
  onkeyup={{fn (action "onUserInteractionAction") @formField "keyDown"}}
  onblur={{fn (action "onUserInteractionAction") @formField "focusOut"}}
  onfocus={{fn (action "onUserInteractionAction") @formField "focusIn"}}
  onchange={{fn (action "onChangeAction") @formField}}
  placeholder={{@formField.placeholder}}
  autofocus={{@formField.autofocus}}
  readonly={{@formField.readonly}}
  disabled={{@formField.disabled}}
  name={{@formField.name}}
  id={{@formField.id}}
  ariaLabelledBy={{@ariaLabelledBy}}
  aria-label={{@ariaLabel}}
/>