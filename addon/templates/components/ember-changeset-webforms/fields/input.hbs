<input
  class="{{ember-changeset-webforms/dynamic-class-names
      'inputElement'
      @changesetWebform
      @formField
      @formField.validationStatus
    }}"
  value={{@displayValue}}
  onkeyup={{fn (action "onUserInteraction") @formField "keyUp"}}
  onkeydown={{fn (action "onUserInteraction") @formField "keyDown"}}
  onblur={{fn (action "onUserInteraction") @formField "focusOut"}}
  onfocus={{fn (action "onUserInteraction") @formField "focusIn"}}
  onchange={{fn (action "onChange") @formField}}
  placeholder={{@formField.placeholder}}
  autofocus={{@formField.autofocus}}
  type={{@formField.inputType}}
  readonly={{@formField.readonly}}
  disabled={{@formField.disabled}}
  name={{@formField.name}}
  id={{@formField.id}}
  ariaLabelledBy={{@ariaLabelledBy}}
  aria-label={{@ariaLabel}}
/>