{{#unless @formField.hideLabel}}
  {{#if @formField.labelComponent}}
    <legend>{{component @formField.labelComponent}}</legend>
  {{else if @formField.fieldLabel}}
    <legend>{{@formField.fieldLabel}}</legend>
  {{/if}}
{{/unless}}