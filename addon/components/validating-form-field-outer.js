import Component from '@ember/component';
import layout from '../templates/components/validating-form-field-outer';

export default Component.extend({
  layout,
  classNames: ['field-container'],
});
