{{input value=(get changeset propertyName) onblur=(action "validateProperty" changeset property)}}
{{!-- Note get helper to interpolate propertyName, like object[key] in js. --}}
  {{#if (get changeset.error propertyName)}}
    {{#each (get (get changeset.error propertyName) 'validation') as |error|}}
      <br>
      {{error}}
    {{/each}}
  {{/if}}
