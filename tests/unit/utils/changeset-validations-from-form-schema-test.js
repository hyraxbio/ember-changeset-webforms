import changesetValidationsFromFormSchema from 'dummy/utils/changeset-validations-from-form-schema';
import { module, test } from 'qunit';

module('Unit | Utility | changeset-validations-from-form-schema', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = changesetValidationsFromFormSchema();
    assert.ok(result);
  });
});
