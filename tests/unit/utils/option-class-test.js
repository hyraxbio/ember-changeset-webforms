import optionClass from 'dummy/utils/option-class';
import { module, test } from 'qunit';

module('Unit | Utility | option-class', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = optionClass();
    assert.ok(result);
  });
});
