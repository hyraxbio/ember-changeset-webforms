import formSettings from 'dummy/utils/form-settings';
import { module, test } from 'qunit';

module('Unit | Utility | form-settings', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = formSettings();
    assert.ok(result);
  });
});
