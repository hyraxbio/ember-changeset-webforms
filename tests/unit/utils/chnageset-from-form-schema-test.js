import chnagesetFromFormSchema from 'dummy/utils/chnageset-from-form-schema';
import { module, test } from 'qunit';

module('Unit | Utility | chnageset-from-form-schema', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = chnagesetFromFormSchema();
    assert.ok(result);
  });
});
