import mergeWithDefaults from 'dummy/utils/merge-with-defaults';
import { module, test } from 'qunit';

module('Unit | Utility | merge-with-defaults', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = mergeWithDefaults();
    assert.ok(result);
  });
});
