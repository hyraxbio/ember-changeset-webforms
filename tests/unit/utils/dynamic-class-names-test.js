import dynamicClassNames from 'dummy/utils/dynamic-class-names';
import { module, test } from 'qunit';

module('Unit | Utility | dynamic-class-names', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dynamicClassNames();
    assert.ok(result);
  });
});
