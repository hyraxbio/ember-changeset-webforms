import dotify from 'dummy/utils/dotify';
import { module, test } from 'qunit';

module('Unit | Utility | dotify', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dotify();
    assert.ok(result);
  });
});
