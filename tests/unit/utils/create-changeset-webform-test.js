import createChangesetWebform from 'dummy/utils/create-changeset-webform';
import { module, test } from 'qunit';

module('Unit | Utility | create-changeset-webform', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = createChangesetWebform();
    assert.ok(result);
  });
});
