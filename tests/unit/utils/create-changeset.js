import createChangeset from 'dummy/utils/create-changeset';
import { module, test } from 'qunit';

module('Unit | Utility | chnageset-from-form-schema', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = createChangeset();
    assert.ok(result);
  });
});
