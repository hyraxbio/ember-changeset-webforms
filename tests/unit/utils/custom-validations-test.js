import customValidations from 'dummy/utils/custom-validations';
import { module, test } from 'qunit';

module('Unit | Utility | custom-validations', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = customValidations();
    assert.ok(result);
  });
});
