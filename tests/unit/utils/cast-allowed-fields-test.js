import castAllowedFields from 'dummy/utils/cast-allowed-fields';
import { module, test } from 'qunit';

module('Unit | Utility | cast-allowed-fields', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = castAllowedFields();
    assert.ok(result);
  });
});
