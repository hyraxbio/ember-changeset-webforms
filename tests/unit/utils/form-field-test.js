import formField from 'dummy/utils/form-field';
import { module, test } from 'qunit';

module('Unit | Utility | form-field', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = formField();
    assert.ok(result);
  });
});
