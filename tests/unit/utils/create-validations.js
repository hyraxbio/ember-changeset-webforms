import createValidations from 'dummy/utils/create-validations';
import { module, test } from 'qunit';

module('Unit | Utility | changeset-validations', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = createValidations();
    assert.ok(result);
  });
});
