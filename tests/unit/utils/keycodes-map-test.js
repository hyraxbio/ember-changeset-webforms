import keycodesMap from 'dummy/utils/keycodes-map';
import { module, test } from 'qunit';

module('Unit | Utility | keycodes-map', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = keycodesMap();
    assert.ok(result);
  });
});
