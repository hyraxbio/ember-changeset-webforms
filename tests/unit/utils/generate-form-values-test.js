import generateFormValues from 'dummy/utils/generate-form-values';
import { module, test } from 'qunit';

module('Unit | Utility | generate-form-values', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generateFormValues();
    assert.ok(result);
  });
});
