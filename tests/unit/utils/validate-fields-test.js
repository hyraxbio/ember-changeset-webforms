import validateFields from 'dummy/utils/validate-fields';
import { module, test } from 'qunit';

module('Unit | Utility | validate-fields', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = validateFields();
    assert.ok(result);
  });
});
