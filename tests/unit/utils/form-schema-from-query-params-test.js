import formSchemaFromQueryParams from 'dummy/utils/form-schema-from-query-params';
import { module, test } from 'qunit';

module('Unit | Utility | form-schema-from-query-params', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = formSchemaFromQueryParams();
    assert.ok(result);
  });
});
