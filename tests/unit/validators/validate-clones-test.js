import { module, test } from 'qunit';
import validateValidateClones from 'ember-pojo-validating-fields/validators/validate-clones';

module('Unit | Validator | validate-clones');

test('it exists', function(assert) {
  assert.ok(validateValidateClones());
});
